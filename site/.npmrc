# Don't run pnpm's deps-status check before scripts. pnpm 10 exits non-zero on the (harmless)
# ignored sharp build script, which was blocking `pnpm dev` from starting Next.
verify-deps-before-run=false
